{
  "name": "Inline Pattern Chained Ambiguity",
  "description": "x ? y ? z — inline `?` is left-associative: (x ? y) ? z, the first match's result is the second's scrutinee",
  "source": "x ? y ? z",
  "expect": "Scope[Pattern(Pattern(Identifier(x),[Branch(Identifier(y),nil)]),[Branch(Identifier(z),nil)])]"
}
