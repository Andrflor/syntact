{
  "name": "Pattern Capture Same Name As Type",
  "description": "x ? { u8(u8) -> u8 } — glued `T(x)` is the capture form (sugar for `T:(x)`), so a capture named like its type stays a capture: Identifier(u8, capture=u8)",
  "source": "x ? { u8(u8) -> u8 }",
  "expect": "Scope[Pattern(Identifier(x),[Branch(Identifier(u8,u8),Identifier(u8))])]"
}
