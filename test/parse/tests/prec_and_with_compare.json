{
  "name": "Precedence And With Comparison Left",
  "description": "a & b < c — COMPARISON > AND. '<' binds tighter than '&', so 'b < c' forms first, then '&' wraps it. Result And(a, Less(b,c)).",
  "source": "a & b < c",
  "expect": "Scope[Operator(And,Identifier(a),Operator(Less,Identifier(b),Identifier(c)))]"
}
