{
  "name": "tok_double_colon_start",
  "description": "'::a' — a leading '::' (cast operator) has no left operand at scope start, so it is skipped by recovery and 'a' parses as Identifier.",
  "source": "::a",
  "expect": "Scope[Identifier(a)]"
}
