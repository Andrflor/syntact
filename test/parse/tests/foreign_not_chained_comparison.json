{
  "name": "Foreign Not Chained Comparison",
  "description": "a<b>c stays (a<b)>c — lexically `<b>` is indistinguishable from a provenance, so the foreign form is recognized ONLY in prefix position; an infix `<` is always a comparison.",
  "source": "a<b>c",
  "expect": "Scope[Operator(Greater,Operator(Less,Identifier(a),Identifier(b)),Identifier(c))]"
}
