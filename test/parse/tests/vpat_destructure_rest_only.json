{
  "name": "Destructure With Rest Only",
  "description": "Foo:{...rest} — single expanded rest binding in destructure.",
  "source": "Foo:{...rest}",
  "expect": "Scope[Carve(Constraint(Identifier(Foo),nil),[Expand(Identifier(rest))])]"
}
