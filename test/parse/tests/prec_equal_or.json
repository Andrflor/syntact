{
  "name": "Precedence Equal With Or",
  "description": "a = b | c — EQUALITY > OR. '=' binds tighter than '|', so 'a = b' forms first, then '| c' wraps it. Result Or(Equal(a,b), c).",
  "source": "a = b | c",
  "expect": "Scope[Operator(Or,Operator(Equal,Identifier(a),Identifier(b)),Identifier(c))]"
}
