{
  "name": "Event Pull No Catch Direct Scope",
  "description": "Log -< { -> 0 } — event pull with no catch identifier, direct scope body; `-> 0` is a Product, as everywhere else",
  "source": "Log -< { -> 0 }",
  "expect": "Scope[EventPull(,Identifier(Log),Scope[Product(Literal(Integer,0))])]"
}
