{
  "name": "Constraint With Name+Capture Followed By Carved Carve",
  "description": "Circle:name(capture){radius->data} — both name and capture together",
  "source": "Circle:name(capture){radius->data}",
  "expect": "Scope[Carve(Constraint(Identifier(Circle),Identifier(name,capture)),[Pointing(Identifier(radius),Identifier(data))])]"
}
