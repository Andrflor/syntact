{"name": "cover_union_unshared_name_rejected", "description": "soundness: a name only ONE operand defines is NOT in the common projection -- the scrutinee might be the other shape, so reading it must not resolve positionally against the operand we happened to pick", "source": "Circle -> {\n  u8:radius\n}\nSquare -> {\n  u8:side\n}\nt -> {\n  (Circle|Square):shape\n  -> shape ? {\n    Circle: | Square: -> radius\n    -> 0\n  }\n}", "expect_errors": ["Undefined_Identifier"]}
