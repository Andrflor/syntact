{"name": "refine_structural_via_machine_scope", "description": "refinement reaches through a machine scope: with Shape -> {-> Circle: -> Square:} the Square branch must narrow to Square, not to the FIRST production's default (fold_constraint must honour the branch override, or the carve looks for `side` in Circle)", "source": "Circle->{u8:radius}\nSquare->{u8:side}\nShape -> {\n  circle -> Circle\n  square -> Square\n  -> circle:\n  -> square:\n}\ndouble -> {\n  Shape:shape\n  -> shape ? {\n    Circle -> shape{radius->.radius*2}\n    Square -> shape{side->.side*2}\n  }\n}", "expect_errors": []}
