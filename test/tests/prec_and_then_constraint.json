{
  "name": "Precedence And Then Constraint",
  "description": "a & b:c — CONSTRAINT > AND. ':' binds tighter than '&', so 'b:c' forms first, then '&' wraps it. Result And(a, Constraint(b,c)).",
  "source": "a & b:c",
  "expect": "Scope[Operator(And,Identifier(a),Constraint(Identifier(b),Identifier(c)))]"
}
