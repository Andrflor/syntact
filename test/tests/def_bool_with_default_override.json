{
  "name": "Default Bool With Override",
  "description": "bool:enabled -> true — shaped binding overridden with the boolean literal 'true'.",
  "source": "bool:enabled -> true",
  "expect": "Scope[Pointing(Constraint(Identifier(bool),Identifier(enabled)),Literal(Bool,true))]"
}
