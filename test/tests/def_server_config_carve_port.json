{
  "name": "Default Server Config Carve Port",
  "description": "ServerConfig:config{port -> 3000} — declaring a binding config of shape ServerConfig whose value is a carve overriding port. Constraint at the top, Carve nested as the value.",
  "source": "ServerConfig:config{port -> 3000}",
  "expect": "Scope[Constraint(Identifier(ServerConfig),Carve(Identifier(config),[Pointing(Identifier(port),Literal(Integer,3000))]))]"
}
