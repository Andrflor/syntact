{
  "name": "Precedence Range Literal With Offset Identifier",
  "description": "1..5 + offset — semantic expectation: the addition binds into the range's right bound. Expect: Range(1, Add(5, offset)).",
  "source": "1..5 + offset",
  "expect": "Scope[Range(Literal(Integer,1),Operator(Add,Literal(Integer,5),Identifier(offset)))]"
}
