{
  "name": "Precedence Or With Comparisons",
  "description": "a > b | c < d — COMPARISON > OR. Both '>' and '<' bind tighter than '|', so the comparisons form first and '|' becomes the root. Result Or(Greater(a,b), Less(c,d)).",
  "source": "a > b | c < d",
  "expect": "Scope[Operator(Or,Operator(Greater,Identifier(a),Identifier(b)),Operator(Less,Identifier(c),Identifier(d)))]"
}
