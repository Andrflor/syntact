{
  "name": "Constraint With Name Followed By Carved Override",
  "description": "Circle:name{radius->data} — name is the bound identifier, then a no-space {...} wraps the whole Constraint in an Override",
  "source": "Circle:name{radius->data}",
  "expect": "Scope[Override(Constraint(Identifier(Circle),Identifier(name)),[Pointing(Identifier(radius),Identifier(data))])]"
}
