{
  "name": "Precedence Range With Add (left)",
  "description": "a..b + c — semantic expectation: the addition binds into the range's right bound. Expect: Range(a, Add(b,c)).",
  "source": "a..b + c",
  "expect": "Scope[Range(Identifier(a),Operator(Add,Identifier(b),Identifier(c)))]"
}
