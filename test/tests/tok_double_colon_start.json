{
  "name": "tok_double_colon_start",
  "description": "'::a' — first ':' has no space before (BOF) and no space after (next is ':') → ConstraintBind. ConstraintBind has no prefix rule → no statement. Same for the second ':'. UNCERTAIN: empty Scope[].",
  "source": "::a",
  "expect": "Scope[]"
}
