{
  "name": "Destructure Nested With Constraint",
  "description": "Foo:{Bar:{x}} — nested destructure where the inner field is itself a constrained destructure. Constraint at the top at each level, with Carve nested as the value.",
  "source": "Foo:{Bar:{x}}",
  "expect": "Scope[Constraint(Identifier(Foo),Carve(nil,[Constraint(Identifier(Bar),Carve(nil,[Identifier(x)]))]))]"
}
