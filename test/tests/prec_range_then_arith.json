{
  "name": "Precedence Range Then Arithmetic",
  "description": "1..5 + 1 — semantic expectation: the addition binds into the range's right bound, matching the convention used by most languages. Expect: Range(1, Add(5,1)).",
  "source": "1..5 + 1",
  "expect": "Scope[Range(Literal(Integer,1),Operator(Add,Literal(Integer,5),Literal(Integer,1)))]"
}
