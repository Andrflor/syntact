{
  "name": "Precedence Range With Multiply",
  "description": "a..b * c — semantic expectation: the multiplication binds into the range's right bound. Expect: Range(a, Multiply(b,c)).",
  "source": "a..b * c",
  "expect": "Scope[Range(Identifier(a),Operator(Multiply,Identifier(b),Identifier(c)))]"
}
