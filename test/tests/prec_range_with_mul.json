{
  "name": "Precedence Range With Multiply",
  "description": "a..b * c — RANGE binds tighter than FACTOR. 'a..b' forms first, then '* c' wraps it. Result Multiply(Range(a,b), c).",
  "source": "a..b * c",
  "expect": "Scope[Operator(Multiply,Range(Identifier(a),Identifier(b)),Identifier(c))]"
}
