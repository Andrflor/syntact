{
  "name": "Constraint With Capture Followed By Carved Override",
  "description": "Circle:(capture){radius->data} — capture-only identifier, then {...} wraps the Constraint in an Override",
  "source": "Circle:(capture){radius->data}",
  "expect": "Scope[Override(Constraint(Identifier(Circle),Identifier(,capture)),[Pointing(Identifier(radius),Identifier(data))])]"
}
