{
  "name": "Constraint With Capture Followed By Carved Carve",
  "description": "Circle:(capture){radius->data} — capture-only identifier, then {...} wraps the Constraint in an Carve",
  "source": "Circle:(capture){radius->data}",
  "expect": "Scope[Carve(Constraint(Identifier(Circle),Identifier(,capture)),[Pointing(Identifier(radius),Identifier(data))])]"
}
