{
  "name": "Precedence Pattern Inline With Add Inside",
  "description": "x ? a + b — the inline pattern's RHS should consume the full arithmetic expression, so '+ b' attaches inside the Branch rather than to the Pattern. WILL_FAIL_UNTIL_FIX: parser currently stops inline-RHS at TERM precedence (a), producing Operator(Add,Pattern(...),b).",
  "source": "x ? a + b",
  "expect": "Scope[Pattern(Identifier(x),[Branch(Operator(Add,Identifier(a),Identifier(b)),nil)])]"
}
