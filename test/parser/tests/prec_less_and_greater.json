{
  "name": "Precedence Less And Greater With Logical And",
  "description": "a < b & c > d — COMPARISON > AND. Both '<' and '>' bind tighter than '&', so the comparisons form first and '&' becomes the root. Result And(Less(a,b), Greater(c,d)).",
  "source": "a < b & c > d",
  "expect": "Scope[Operator(And,Operator(Less,Identifier(a),Identifier(b)),Operator(Greater,Identifier(c),Identifier(d)))]"
}
