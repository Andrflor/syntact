{
  "name": "Z CompileTime On External",
  "description": "!@lib — CompileTime prefix consumes External (PRIMARY) entirely.",
  "source": "!@lib",
  "expect": "Scope[CompileTime(External(lib,nil))]"
}
