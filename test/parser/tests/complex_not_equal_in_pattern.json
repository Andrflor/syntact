{
  "name": "Not Equal Combined With And In Constraint",
  "description": "a != b & c — EQUALITY (where != lives, same level as =) binds tighter than AND. 'a != b' forms first, then '& c' wraps it. Result And(NotEqual(a,b), c).",
  "source": "a != b & c",
  "expect": "Scope[Operator(And,Operator(NotEqual,Identifier(a),Identifier(b)),Identifier(c))]"
}
