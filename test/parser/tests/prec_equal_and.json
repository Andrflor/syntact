{
  "name": "Precedence Equal With And",
  "description": "a = b & c — EQUALITY > AND. '=' binds tighter than '&', so 'a = b' forms first, then '& c' wraps it. Result And(Equal(a,b), c).",
  "source": "a = b & c",
  "expect": "Scope[Operator(And,Operator(Equal,Identifier(a),Identifier(b)),Identifier(c))]"
}
