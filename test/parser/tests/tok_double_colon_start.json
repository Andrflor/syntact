{
  "name": "tok_double_colon_start",
  "description": "'::a' — the two colons are consumed/skipped by recovery, then 'a' parses as Identifier.",
  "source": "::a",
  "expect": "Scope[Identifier(a)]"
}
