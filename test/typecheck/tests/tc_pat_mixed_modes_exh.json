{ "name": "tc_pat_mixed_modes_exh", "description": "mixed value + typecheck branches: =0..120 is a value-match (scrutinee must statically BE 0..120), not a typecheck cover, so the union does not cover u8: non-exhaustive", "source": "a -> ??::u8\nr -> a ? {\n  =0..120 -> 1\n  121..255 -> 2\n}\n-> r", "expect_errors": ["Non_Exhaustive_Pattern"] }
