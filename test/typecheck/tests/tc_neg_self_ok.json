{ "name": "tc_neg_self_ok", "description": "a bare ~10 constraint demands MEMBERSHIP (a concrete value != 10), not set-inclusion. The set ~10 is not itself a member of ~10 (like u8:a -> u8), so the value ~10 is rejected. Use =~10:a -> ~10 for strict equality", "source": "~10:a -> ~10", "expect_errors": ["Constraint_Mismatch"] }
