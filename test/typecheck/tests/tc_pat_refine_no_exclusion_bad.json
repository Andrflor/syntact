{ "name": "tc_pat_refine_no_exclusion_bad", "description": "without a pattern excluding the low bound, a recursive carve n-1 over 1..255:n reinjects 0, which 1..255 rejects: refinement must NOT mask this", "source": "f -> {\n  1..255:n\n  -> f{n -> n - 1}\n}\n-> 0", "expect_errors": ["Constraint_Mismatch"] }
