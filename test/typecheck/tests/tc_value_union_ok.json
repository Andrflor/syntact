{ "name": "tc_value_union_ok", "description": "a bare ~10 constraint demands MEMBERSHIP: a single concrete value != 10. The union 5 | 11 is a set of two values, not one member, so it is rejected (same rule as ~(10|11): a set is never a member of a bare constraint)", "source": "~10:a -> 5 | 11", "expect_errors": ["Constraint_Mismatch"] }
