{ "name": "tc_neg_subset_ok", "description": "a bare ~10 constraint demands MEMBERSHIP, not set-inclusion. ~(10|11) is itself a set, not a concrete value member of ~10, so it is rejected (set-inclusion ~(10|11) ⊆ ~10 is irrelevant to a bare constraint)", "source": "~10:a -> ~(10 | 11)", "expect_errors": ["Constraint_Mismatch"] }
