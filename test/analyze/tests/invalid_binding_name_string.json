{
  "name": "Invalid binding name string",
  "description": "'x' -> 5 — the left of a binding must be a name, not a string literal",
  "source": "'x' -> 5",
  "expect_errors": ["Invalid_Binding_Name"]
}
