{
  "name": "Property on a collapse resolves against the production",
  "description": "Regression: `avg{red blue}!.r` must analyze clean — resolve_property_site reads a collapse through execute_production, so the output shape is not lost",
  "source": "Color -> {\n  u8:r\n  u8:g\n  u8:b\n}\navg -> {\n  Color:a\n  Color:b\n  -> Color{\n    r -> (a.r + b.r)/2\n    g -> (a.g + b.g)/2\n    b -> (a.b + b.b)/2\n  }\n}\nColor:red{r->255}\nColor:blue{b->255}\n-> avg{red blue}!.r",
  "expect_errors": []
}
