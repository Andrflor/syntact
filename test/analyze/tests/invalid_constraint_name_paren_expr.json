{
  "name": "Invalid constraint name paren expr",
  "description": "u8:(a+b) -> 0 — a parenthesized EXPRESSION cannot name a colored binding. (A bare `(a)` is the anonymous-capture form and is legal — see valid_anonymous_capture.)",
  "source": "z -> {u8:(a+b) -> 0}",
  "expect_errors": ["Invalid_Constraint_Name"]
}
