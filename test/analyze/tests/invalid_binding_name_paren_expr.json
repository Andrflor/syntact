{
  "name": "Invalid binding name paren expr",
  "description": "(a+b) -> 5 — the left of a binding must be a name, not an arithmetic expression",
  "source": "a -> 1\nb -> 2\n(a+b) -> 5",
  "expect_errors": ["Invalid_Binding_Name"]
}
